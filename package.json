{
  "name": "contract-subgraph",
  "version": "0.1.0",
  "description": "Subgraph for contract reviewer using substreams",
  "main": "index.js",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ contract-subgraph",
    "create-local": "graph create --node http://localhost:8020/ contract-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ contract-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 contract-subgraph",
    "package-substreams": "./scripts/package-substreams.sh"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.51.0",
    "@graphprotocol/graph-ts": "^0.30.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
